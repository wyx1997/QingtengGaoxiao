<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_search_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="7dp"
    android:focusable="true"
    android:clickable="true">

    <ImageView
        android:id="@+id/main_search_img"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:src="@drawable/main_search"
        android:layout_marginRight="5dp"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/main_search_text" />

    <TextView
        android:id="@+id/main_search_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/main_search_title"
        app:layout_constraintLeft_toRightOf="@id/main_search_img"
        app:layout_constraintRight_toRightOf="parent"/>

</android.support.constraint.ConstraintLayout>